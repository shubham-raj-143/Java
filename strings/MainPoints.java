package strings;

public class MainPoints {
	
	/* 1>> In Java, the String equals() method compares the two given strings based on the data/content of the string.
	 * 	   If all the contents of both the strings are the same then it returns true. If all characters are not matched
	 * 	   then it returns false.

			contains method -> returns true if the substring is present in the main string

				For ex:

    				String a = "Jarvis123";
    				String b = "Jarvis";

    				System.out.println(a.contains(b));  // will return true
    				System.out.println(a.equals(b));    // will return false
	 * 
	 */

}
